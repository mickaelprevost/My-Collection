{% extends 'base.html.twig' %}

{% block title %}Universe
{% endblock %}

{% block body %}
	<h1 class="mt-3 mb-1">Universe</h1>

	<a class="btn btn-primary mt-3 mb-3" href="{{ path('app_back_universe_new') }}">Ajouter un univers</a>
	<a class="btn btn-primary mt-3 mb-3" href="{{ path('app_back_universe_index') }}">Retour à la liste des univers</a>

	<table class="table">
		<thead>
			<tr>
				<th>Id</th>
				<th>Nom</th>
			</tr>
		</thead>
		<tbody>
			<tr>
				<td>{{ universe.id }}</td>
				<td>{{ universe.name }}</td>
			</tr>
		</tbody>
	</table>

	<a class="btn btn-secondary mt-3 mb-4" href="{{ path('app_back_universe_edit', {'id': universe.id}) }}">Modifier</a>

	{{ include('back/universe/_delete_form.html.twig') }}
{% endblock %}
