<nav class="navbar navbar-expand-lg">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{path("app_home")}}">
			<svg xmlns="http://www.w3.org/2000/svg" width="40" height="40" viewBox="0 0 512 512"><path fill="Black" d="M261.56 101.28a8 8 0 0 0-11.06 0L66.4 277.15a8 8 0 0 0-2.47 5.79L63.9 448a32 32 0 0 0 32 32H192a16 16 0 0 0 16-16V328a8 8 0 0 1 8-8h80a8 8 0 0 1 8 8v136a16 16 0 0 0 16 16h96.06a32 32 0 0 0 32-32V282.94a8 8 0 0 0-2.47-5.79Z"/><path fill="#CF2A27" d="m490.91 244.15l-74.8-71.56V64a16 16 0 0 0-16-16h-48a16 16 0 0 0-16 16v32l-57.92-55.38C272.77 35.14 264.71 32 256 32c-8.68 0-16.72 3.14-22.14 8.63l-212.7 203.5c-6.22 6-7 15.87-1.34 22.37A16 16 0 0 0 43 267.56L250.5 69.28a8 8 0 0 1 11.06 0l207.52 198.28a16 16 0 0 0 22.59-.44c6.14-6.36 5.63-16.86-.76-22.97Z"/></svg>
		</a>
		<div class="justify-content-end">
			<ul class="navbar-nav">
				<li class="nav-item dropdown">
					{# le user est-il connecté ? #}
					{# {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %} #}
						{% if is_granted('IS_AUTHENTICATED_FULLY') %}

							<button class="btn btn-dark dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">  <p class="show hide">{{ app.user.username }}</p>
							{% if (app.user.picture is not empty) %}
								<td><img class="rounded-circle" src="{{asset('upload/usersPics/' ~ app.user.picture)}}" alt="logo utilisateur" height="40"></td>
							{% else %}
								<td><img class="rounded-circle" src="{{ asset('asset/images/picto.png') }}" height="40"></td>
							{% endif %}
						</button>

						{# connecté #}
						<ul class="dropdown-menu">
							{% if is_granted('IS_AUTHENTICATED_FULLY') %}
								<li>
									<a class="dropdown-item" href="{{path("app_user_index", {'id': app.user.id}) }}">Profil</a>
								</li>
							{% endif %}

							{% if is_granted('ROLE_ADMIN') %}


								<li>
									<a class="dropdown-item" href="{{path("app_back_home")}}">BackOffice</a>
								</li>
							{% endif %}
							<li>
								<a class="dropdown-item" href="{{path("app_messagerie_index")}}">Messagerie ({{ app.user.received|length }})</a>
							</li>
								<li>
								<a class="dropdown-item" href="{{path("app_contacts_list")}}">Contacts</a>
							</li>
							<li><hr class="dropdown-divider"></li>
							<li>
								<a class="dropdown-item" href="{{path("logout")}}">Se déconnecter</a>
							</li>
						</ul>
					{% else %}
						<button class="btn btn-dark dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">S'inscrire / Se connecter</button>
						{# non connecté #}
						<ul class="dropdown-menu">
							<li>
								<a class="dropdown-item" href="{{path("app_signup")}}">S'inscrire</a>
							</li>
							<li>
								<a class="dropdown-item" href="{{path("login")}}">Se connecter</a>
							</li>
						</ul>
					{% endif %}
				</li>
			</li>
		</ul>
	</div>
</div></nav>{# <div class="collectible-right-box-bot"> #}{# <a class="item-test" href="{{ path('app_user_index', {'id': user.id})}}">
		                                <img class="rounded-user item-test" src="{{asset('upload/userPics' ~ user.picture)}}" alt="alternative">
		                            </a>
		
		                            <a class="item-test" href="{{ path('app_user_index', {'id': user.id})}}">Par {{ user.username }}</a> #}{# </div> #}

					
